// Cấu hình môi trường production (dùng khi `ng build --configuration production`).
// TODO: đổi apiUrl thành domain API thật khi deploy (VD: 'https://api.aurora-resort.com/api').
export const environment = {
  production: true,
  apiUrl: '/api'
};
